
/* IMPORT */

import * as React from 'react';

/* NOTE BADGE */

const NoteBadge = ({ icon }) => (
  <div className="icon-badge">
    <i className="icon">{icon}</i>
  </div>
);

/* EXPORT */

export default NoteBadge;
